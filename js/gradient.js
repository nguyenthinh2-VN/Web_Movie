window.addEventListener('load', function () {
    // Lấy phần tử loading
    const loading = document.getElementById('loading');

    // Ẩn phần tử loading sau khi trang đã tải xong
    loading.style.display = 'none';
});